{
  "name": "rc-vault-v10",
  "version": "10.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node tests/lookup-mappers.test.mjs",
    "check": "node --check api/lookup.js && node --check api/health.js"
  }
}
